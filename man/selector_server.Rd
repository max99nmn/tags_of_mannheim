% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_selector.R
\name{selector_server}
\alias{selector_server}
\title{Tag Selector Server Module (Selectize Version)}
\usage{
selector_server(id, data_for_selector)
}
\arguments{
\item{id}{A `character` string. The namespace ID for the module.}

\item{data_for_selector}{A reactive `data.frame`. Must contain `tag_id`
(the values) and `tag_name` (the labels) columns.}
}
\value{
A `reactive` expression containing a character vector of the
  selected tag IDs.
}
\description{
The server-side logic for the `selectizeInput` tag selector. This function
populates the selector's choices, pre-selects a random number of items
up to a defined maximum, and returns the user's current selection.
}
\note{
This function depends on a globally defined variable,
  `maximum_selector_items`, to control the number of randomly pre-selected
  tags. It also uses `server = TRUE` for better performance with large
  choice sets.
}
